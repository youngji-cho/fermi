import React from 'react';
import ReactDOM from 'react-dom';
import {Layout} from './components/layout';

ReactDOM.render(<Layout />, document.getElementById('body'));
//ReactDOM.render(<Drawer />, document.getElementById('layout'));
